<%# search form moved below the fixed A–Z bar spacer %>

<%# Install banner (hidden by default, toggled by JS) %>
<div id="install-banner" class="hidden fixed bottom-0 inset-x-0 z-50">
  <div class="alert shadow bg-base-100 border border-base-300 rounded-t-xl">
    <div class="flex-1">
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="w-6 h-6 stroke-current"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v12m0 0l-3-3m3 3l3-3M4 20h16"/></svg>
      <span>Instala esta app en tu teléfono para un acceso rápido.</span>
    </div>
    <div class="flex items-center gap-2">
      <button id="install-action" class="btn btn-primary btn-sm" onclick="(function(){var tip=document.getElementById('ios-tip'); if(tip){tip.classList.remove('hidden');} var m=document.getElementById('install-modal'); if(m && m.showModal){try{m.showModal();}catch(e){}}})();">Instalar</button>
      <button id="install-dismiss" class="btn btn-ghost btn-sm" onclick="try{localStorage.setItem('hm_install_banner_dismissed_v2','1');}catch(e){} this.closest('#install-banner').classList.add('hidden'); return false;">No ahora</button>
    </div>
  </div>
  <div id="ios-tip" class="hidden alert bg-base-100 border-t border-base-300 rounded-none">
    <span>En iPhone: toca Compartir → “Añadir a pantalla de inicio”.</span>
  </div>
  <div class="divider m-0"></div>
  
  <dialog id="install-modal" class="modal">
    <div class="modal-box">
      <h3 class="font-bold text-lg mb-2">Cómo instalar en iPhone</h3>
      <ol class="list-decimal ml-5 space-y-2 text-sm">
        <li>Toca el botón <strong>Compartir</strong> en la barra inferior.</li>
        <li>Desliza hacia abajo y selecciona <strong>“Añadir a pantalla de inicio”</strong>.</li>
        <li>Confirma el nombre y pulsa <strong>Añadir</strong>.</li>
      </ol>
      <div class="modal-action">
        <form method="dialog">
          <button class="btn">Cerrar</button>
        </form>
      </div>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>

  <script>
    (function(){
      try {
        var ua = navigator.userAgent || '';
        var isIOS = /iphone|ipad|ipod/i.test(ua);
        var standalone = (window.matchMedia && window.matchMedia('(display-mode: standalone)').matches) || window.navigator.standalone === true;
        var dismissed = localStorage.getItem('hm_install_banner_dismissed_v2') === '1';
        var onAppPath = location.pathname.indexOf('/app') === 0;
        if (isIOS && !standalone && !dismissed && onAppPath) {
          var banner = document.getElementById('install-banner');
          if (banner) banner.classList.remove('hidden');
          // Keep instructions hidden until user taps Install (handled by application.js)
        }
      } catch(e) {}
    })();
  </script>
  </div>

<%# Page header with church/account name (scrolls away) %>
<div class="max-w-screen-lg px-2 mb-2">
  <% account_name = ActsAsTenant.current_tenant&.name || 'Tu Iglesia' %>
  <div class="card">
    <div class="card-body py-3 px-4">
      <h1 class="text-xl sm:text-2xl font-extrabold">
        Canciones de la Iglesia <span class="text-primary"><%= account_name %></span>
      </h1>
    </div>
  </div>
</div>

<%# Sticky A–Z index and search (remains visible) %>
<div class="sticky top-0 left-0 right-0 z-40 bg-base-100/95 backdrop-blur border-b border-base-300">
  <!-- Letters row: single-line, scrollable on mobile -->
  <div class="px-2 py-2">
    <div class="flex flex-wrap items-center gap-1 sm:gap-2" data-controller="letter-nav">
      <%- grouped_songs.keys.sort.each do |letter| %>
        <%= link_to "#letter-#{letter}", 
            class: "btn btn-ghost btn-xs letter-btn", 
            data: { 
              action: "click->letter-nav#selectLetter",
              letter: letter,
              target: "letter-nav.letter" 
            } do %>
          <%= letter %>
        <% end %>
      <%- end %>
    </div>
  </div>
  <!-- Search row: full-width input + button; label hidden -->
  <div class="px-2 pb-2" data-controller="main">
    <%= search_form_for @q, url: app_url, html: { class: "w-full" } do |f| %>
      <label class="sr-only" for="q_title_or_content_cont">Buscar canciones</label>
      <div class="join w-full">
        <%= f.search_field :title_or_content_cont,
          placeholder: "Busca por título o letra…",
          class: "input input-bordered join-item w-full",
          type: "text"
        %>
        <%= f.submit "Buscar", class: "btn btn-primary join-item" %>
      </div>
    <% end %>
  </div>
</div>
<div class="divider"></div>

<% if @songs.any? %>
<div class="flex flex-col w-full gap-10 mt-3">
  <%- grouped_songs.each_pair do |letter, songs| %>
    <div id="letter-<%= letter %>" class="scroll-mt-16" data-letter-section="<%= letter %>">
      <div class="-mx-2 px-2 py-2 bg-base-100/95 border-b border-base-300">
        <h2 class="text-2xl font-extrabold text-primary"><%= letter %> <span class="badge badge-neutral align-middle ml-2"><%= songs.size %></span></h2>
      </div>
      <div class="mt-2 flex flex-col gap-3">
        <%- songs.each do |song| %>
          <details class="group collapse collapse-arrow bg-base-100 border border-base-300 rounded-xl transition duration-200 hover:border-primary/60 hover:shadow-sm">
            <summary class="collapse-title list-none font-semibold text-blue-600 flex items-center justify-between">
              <%= song.title %>
              <span class="badge badge-ghost group-open:hidden">ver letra</span>
            </summary>
            <div class="collapse-content pt-0">
              <article class="prose max-w-none mt-2">
                <%= simple_format song.content %>
              </article>
            </div>
          </details>
        <%- end %>
      </div>
    </section>
  <%- end %>
</div>
<% else %>
  <div class="mx-auto max-w-screen-lg px-2 mt-6">
    <div class="card bg-base-100 border border-base-300 shadow-sm">
      <div class="card-body items-center text-center">
        <h3 class="card-title">No se encontraron canciones</h3>
        <% if params[:q].present? %>
          <p class="text-sm opacity-70">Intenta ajustar tu búsqueda.</p>
          <%= link_to 'Limpiar búsqueda', app_path, class: 'btn btn-ghost btn-sm mt-2' %>
        <% else %>
          <p class="text-sm opacity-70">Aún no hay canciones.</p>
        <% end %>
      </div>
    </div>
  </div>
<% end %>