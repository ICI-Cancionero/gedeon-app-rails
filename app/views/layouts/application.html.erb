<!DOCTYPE html>
<html data-theme="light">
  <head>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="theme-color" content="#0b1020">
    <%# apple-mobile-web-app-capable is declared via the manifest (display: standalone).
        Having it as a meta tag causes iOS to reload the page on orientation change. %>
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <link rel="manifest" href="/app/manifest.webmanifest">
    <!-- Nunito font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,300..1000;1,300..1000&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag 'audio_player', media: 'all' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>
  <body class="pb-24 sm:pb-28">
    <main class="phone-6">
      <%= yield %>
    </main>

    <%# Audio Player - will be shown on pages that include it %>
    <% if content_for?(:audio_player) %>
      <%= yield :audio_player %>
    <% end %>

    <!-- YouTube IFrame API will be loaded by the audio player controller -->
  </body>
</html>
